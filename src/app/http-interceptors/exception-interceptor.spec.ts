import { ExceptionInterceptor } from './exception-interceptor';

describe('ExceptionInterceptor', () => {
  it('should create an instance', () => {
    expect(new ExceptionInterceptor()).toBeTruthy();
  });
});
